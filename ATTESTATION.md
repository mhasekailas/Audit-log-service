# Attestation

**Full Name:** Kailas Mhase  
**Email Address:** mhasekailas@gmail.com  
**Assignment Title:** Audit log service  
**Start Date:** 08/14/2026  
**Submission Date:** 08/18/2026  

I, Kailas Mhase, attest that this submission is my own individual work, completed on my own machine and accounts, and that it honestly reflects my development process and use of AI.

## Revision Identity

> Fill in the values below with the exact commit that matches the submitted ZIP/archive
> before final submission (`git remote get-url origin`, `git rev-parse --abbrev-ref HEAD`,
> `git rev-parse HEAD`). A ZIP without this information cannot be tied to a reviewable
> revision.

| Field | Value |
|---|---|
| Repository URL | _fill in before submission_ |
| Branch | _fill in before submission_ |
| Commit SHA | _fill in before submission_ |
| Working tree state | _clean / dirty - fill in_ |

## Claim-to-Evidence Matrix

| Claim | Evidence | How to reproduce |
|---|---|---|
| Role-based access control (reader/writer/admin) enforced on every audit endpoint | `backend/src/main/java/com/schwab/auditlog/config/SecurityConfig.java`; tests in `AuditLogControllerSecurityTest` | `mvn test -Dtest=AuditLogControllerSecurityTest` |
| Unauthenticated/wrong-role requests are rejected (401/403) | `AuditLogControllerSecurityTest` (`*WithoutCredentialsIsUnauthorized`, `*CannotCreateEvents`, etc.) | `mvn test -Dtest=AuditLogControllerSecurityTest` |
| Replay/duplicate-submission protection via `Idempotency-Key` | `AuditEventService.createEvent`; `duplicateIdempotencyKeyReturnsOriginalEventInsteadOfDuplicate` | `mvn test -Dtest=AuditLogControllerSecurityTest#duplicateIdempotencyKeyReturnsOriginalEventInsteadOfDuplicate` |
| Per-client rate limiting (429 on abuse) | `RateLimitingFilter`; `exceedingRateLimitReturns429` | `mvn test -Dtest=AuditLogControllerSecurityTest#exceedingRateLimitReturns429` |
| Oversized request bodies rejected (413) before DB work | `RequestSizeLimitFilter`; `oversizedPayloadReturns413` | `mvn test -Dtest=AuditLogControllerValidationTest#oversizedPayloadReturns413` |
| Malformed JSON / missing fields / bad path params return 400, not a stack trace | `GlobalExceptionHandler`; `AuditLogControllerValidationTest` | `mvn test -Dtest=AuditLogControllerValidationTest` |
| Server errors never leak exception/SQL detail to clients | `GlobalExceptionHandler#handleDataAccess`; `databaseFailureDuringQueryReturnsGenericServerErrorWithoutLeakingDetails` | `mvn test -Dtest=AuditLogControllerValidationTest#databaseFailureDuringQueryReturnsGenericServerErrorWithoutLeakingDetails` |
| Concurrent writers cannot race the hash-chain tail (contiguous sequence, no duplicates) | `AuditEventService#createEvent` (synchronized chain-write section); `AuditEventConcurrencyTest` | `mvn test -Dtest=AuditEventConcurrencyTest` |
| Chain verification detects content tampering, chain-hash tampering, and deleted-middle records | `AuditEventServiceTest` (`verifyChainDetects*`) | `mvn test -Dtest=AuditEventServiceTest` |
| `ddl-auto=validate` (no silent schema drift), `show-sql=false` in the shipped config | `backend/src/main/resources/application.properties` | Inspect file directly |
| Secrets sourced from environment variables only, never committed | `SecurityConfig#userDetailsService`, `application.properties` (`${AUDIT_DB_USERNAME}` etc.) | Inspect file directly |
| Full Surefire + JaCoCo coverage artifacts generated by a single command | `backend/target/surefire-reports/`, `backend/target/site/jacoco/index.html` | `mvn test` (see Results section below) |

## Latest Verified Test Run

- Command: `mvn test` (run from `backend/`)
- Result: `Tests run: 40, Failures: 0, Errors: 0, Skipped: 0` — `BUILD SUCCESS`
- Artifacts: `backend/target/surefire-reports/*.txt`, `backend/target/site/jacoco/index.html`, `backend/target/site/jacoco/jacoco.csv`
- Known limitation: the chain-write lock (`synchronized` block in `AuditEventService`) serializes writers within a single JVM instance; a multi-node deployment still needs a DB-level locked chain-tail row or transactional serialization (tracked as a follow-up, see `README.md` security section).
